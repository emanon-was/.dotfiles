#compdef dotfiles-cp

local -a commands

commands=(
  'apply:copy source tree entries into destination'
  'unapply:remove unchanged destination copies'
  'help:show usage'
)

case "$CURRENT" in
  2)
    _describe 'dotfiles-cp command' commands
    ;;
  *)
    case "${words[2]:-}" in
      apply|unapply)
        _arguments \
          '--src[source tree]:source tree:_files -/' \
          '--dest[destination tree]:destination tree:_files -/' \
          '--dry-run[show planned operations without changing the filesystem]'
        ;;
    esac
    ;;
esac
